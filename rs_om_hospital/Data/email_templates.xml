<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="email_template_appointment" model="mail.template">
            <field name="name">Appointment Email</field>
            <field name="model_id" ref="model_hospital_appointment"/>
            <field name="subject">Appointment Confirmation</field>
            <field name="email_from">${object.doctor_id.email}</field>
            <field name="email_to">${object.email}</field>
            <field name="body_html"><![CDATA[
                <p>Dear ${object.patient_id.name},</p>
                <p>Your appointment with ${object.doctor_id.name} has been scheduled.</p>
                <p>Appointment Details:</p>
                <ul>
                    <li>Date: ${object.appointment_time.strftime('%Y-%m-%d')}</li>
                    <li>Time: ${object.appointment_time.strftime('%H:%M')}</li>
                    <li>Doctor: ${object.doctor_id.name}</li>
                </ul>
                <p>Please arrive on time for your appointment.</p>
            ]]></field>
        </record>
    </data>
</odoo>



